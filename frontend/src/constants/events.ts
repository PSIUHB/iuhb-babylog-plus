/**
 * Constants for application-wide events
 */
export const EVENTS = {
  /**
   * Emitted when a trackable is created
   */
  TRACKABLE_CREATED: 'trackable:created',
  
  /**
   * Emitted when a trackable is updated
   */
  TRACKABLE_UPDATED: 'trackable:updated',
  
  /**
   * Emitted when a trackable is deleted
   */
  TRACKABLE_DELETED: 'trackable:deleted',
};